
class URLServices {
    public evaluateGitHubURL(url: string): string {
        return "GitHubEvaluator";
    }

    public evaluateNpmURL(url: string): string {
        return "NpmEvaluator";
    }
}